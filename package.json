{
  "name": "reams",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "browserify test/*.test.js -t reactify -d | jsdom-eval",
    "watch": "watchify index.js -o dist/bundle.js -t reactify -d",
    "bundle": "browserify index.js -t reactify -o dist/bundle.js -d"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dom-event-stream": "0.0.1",
    "dom-events": "^0.1.1",
    "dom-sandbox": "0.0.1",
    "dom-value-stream": "0.0.1",
    "mocha": "^2.1.0",
    "objectstate": "^2.0.3",
    "react": "^0.12.2",
    "tape": "^3.5.0",
    "through": "^2.3.6",
    "xtend": "^4.0.0"
  },
  "devDependencies": {
    "reactify": "^1.0.0"
  }
}
